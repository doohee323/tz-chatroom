# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                	controllers.Application.index()

POST		/room															controllers.Application.chatRoom()
GET     /rooms                            controllers.Application.chatRoom()
GET     /room                            	controllers.Application.chatRoom(chatroom)
GET     /room/chat/:username              controllers.Application.chat(username)
GET     /assets/javascripts/chatroom.js  	controllers.Application.chatRoomJs(username)

# Map static resources from the /public folder to the /assets URL path
GET     /*file               controllers.Assets.at(path="/public", file)
 