# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                	controllers.Application.index()

POST		/room															controllers.Application.saveChatRooms()
GET     /rooms                            controllers.Application.chatRooms()
GET     /room/:chatroom                   controllers.Application.chatRoom(chatroom)
GET     /room/chat/:username              controllers.Application.chat(username)

# Map static resources from the /public folder to the /assets URL path
GET     /*file               controllers.Assets.at(path="/public", file)
 